version: "2"
services:
  db:
    image: postgres

    networks:
      - test_network

    environment:
      POSTGRES_PASSWORD: password
      
    volumes:
      - ${HOME}/data/postgresql:/var/lib/postgresql/data      


  elm-dev:
    container_name: elm-dev

    build:
      context: ./build
      dockerfile: Dockerfile

    networks:
      - test_network

networks:
  test_network:

